/**
 * Site-wide details. Edit here and every page updates.
 *
 * TODO — the email below is temporary. Resend can only deliver to
 * plford2000@gmail.com until annabelart.com is verified as a sending domain.
 * Once that verification is done, switch this to Annabel's own address
 * (and update INQUIRY_TO / INQUIRY_FROM in Cloudflare to match).
 */
export const site = {
	email: 'plford2000@gmail.com',
	instagramHandle: 'akd_artt',
};

export const instagramUrl = `https://instagram.com/${site.instagramHandle}`;
