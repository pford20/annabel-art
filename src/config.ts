/**
 * Site-wide details. Edit here and every page updates.
 *
 * TODO — both values below are PLACEHOLDERS. Replace with Annabel's real
 * email address and Instagram handle before the site goes live.
 */
export const site = {
	email: 'hello@example.com',
	instagramHandle: 'annabelart',
};

export const instagramUrl = `https://instagram.com/${site.instagramHandle}`;
